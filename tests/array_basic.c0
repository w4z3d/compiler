int main() {
    int[] arr = alloc_array(int, 5);

    // Fill array
    arr[0] = 5;

    // Sum sorted array
    int sum = 0;
    for (int i = 0; i < 5; i += 1) {
        sum += arr[i];
    }

    return sum;
}
