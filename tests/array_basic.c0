int main() {
    int[] arr = alloc_array(int, 5);

    // Fill array
    arr[0] = 5;
    arr[1] = 3;
    arr[2] = 4;
    arr[3] = 1;
    arr[4] = 2;

    // Sum sorted array
    int sum = 0;
    for (int i = 0; i < 5; i += 1) {
        sum += arr[i];
    }

    return sum;
}
